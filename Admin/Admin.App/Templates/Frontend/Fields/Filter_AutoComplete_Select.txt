  <!-- [AutoCompleteCapital]AutoComplete Start -->
  <div class="form-control">
    <div class="row">
     <div class="position-relative" (focusout)="hide[AutoCompleteCapital]Overlay()">
      <div class="col -4">
        <label>Component</label>
      </div>
      <div class="col -8">
        <div class="input-group position-relative">
          <input
            id="[AutoCompleteSmall]Control"
            type="text"
            class="form-control"
            [(ngModel)]="result.[AutoCompleteCapital]Name"
            (ngModelChange)="on[AutoCompleteCapital]Input($event)"
            (focus)="(result.[AutoCompleteCapital]Name || '').length >= 2 ? show[AutoCompleteCapital] = true : show[AutoCompleteCapital] = false"
            autocomplete="off"
            placeholder="Type at least 2 characters..." />
          <span class="input-group-text" *ngIf="isLoading.autoComplete">
            <span class="spinner-border spinner-border-sm"></span>
          </span>
        </div>
        <ul class="dropdown-menu show w-100 shadow-sm" style="max-height: 200px; overflow-y: auto;"
            *ngIf="show[AutoCompleteCapital] && [AutoCompleteSmall]Options.length > 0">
            <li *ngFor="let option of [AutoCompleteSmall]Options">
                <button type="button"
                    class="dropdown-item d-flex justify-content-between align-items-center"
                    (mousedown)="$event.preventDefault(); select[AutoCompleteCapital](option)">
                    <span>{{ option.name }}</span> <small class="text-muted">ID: {{ option.id }}</small>
                </button>
            </li>
        </ul>
        <ul class="dropdown-menu show w-100" *ngIf="show[AutoCompleteCapital] && !isLoading.autoComplete && [AutoCompleteSmall]Options.length === 0">
            <li><span class="dropdown-item-text text-muted">Please select a valid Component.</span></li>
        </ul>
      </div>
     </div>
    </div>
  </div>
  <!-- [AutoCompleteCapital] AutoComplete End -->