1- Created records in master UI, master DB
2- Create code by Admin app. => create code only
3- Create Migration (set security off, migration on)
4- update database (test_migration Db) => change database to only 1 database
5- run Api, go to Admin controller / apply migration to all tenants databases => change DB for all tenants



CustomReports url field in the database is saved in the format [system_prefix]/[component-name]/list. i.e.:             "url": "bfs/system-action/list/"
the url is searched against all app routes. it finds the url within the [system] Routes. ie if it starts with bfs, the route will be found under the system that has the prefix bfs. (infrastructure-apiService)
if the url starts with mstr then it will be found in master api service, and it will use its api-url. export const REPORTS_ROUTES: Routes = [
    {
        path: 'mstr/report/structure-compare/:id',
        component: StructureCompareComponent,
        canActivate: [RouteGuardService],
        data: { role: ['admin'], api: ['master'], app: ['b.ofc'] } 
    },

logos, missing fields in tenant, system, app  (logo field, abbreviation for any thing related to permissions system, app, action, component)

fix "Admin: Code-dont-Delete" issue
lookups in one promise
Aspire (explore .ServiceDefaults instrumentations)


update databases (action per system) *** Migration need to be genertated first. set flag IsMigration = true
activate the tenant-database when a request come to Api

tables prefix!!
Seed fields in one table (category / subcategory)
Soft Delete

Resource security=> where conditions on table query \ report query. investorId=
link\unlink users

AI Generate data
string computed field, as app name = system name - app name, bfsField add field computeFormula=`${systemName}-${name}`

Record validation
Table validation

autocomplete
Create tenant: create record in master, link it to Auth, run init & seed scripts to define Roles and add bfs.Admin

deals with secrets
linear reports for stores
uploads to a tenant blob

bugs:

Improvements
After crud > ui [Back] button instead of cancel
Back to previuos filtered list

Important!!
Deployment
logging

http://localhost:2400/

sql script:
when adding a new tenant
- add user: Bfs Admin Randa, Suzette to the following tables:
- athUser using the same aspnetId as in master
- athRoleUser the using athUser.Id link to the RoleId = 1
- dont add the role bfsadmin to the athRole

You’re building multi-tenant, permission-based, JWT-backed API. 

Flow here is end-to-end:
------------------------------
- lifetimes (scoped vs singleton)
- permission caching
- tenant resolution
- JWT + authorization handler integration
1. High-level design
---------------------
Per request (scoped):
- Resolve tenant (ITenantProvider)
- Use IPermissionProvider to get permissions for that tenant
- Authorization handler (MultiClaimRequirementHandler) checks if the current user is allowed to call the endpoint

App-wide (singleton):
-Cache permissions per tenant in memory (IMemoryCache or ConcurrentDictionary)
----------------------------------------------
- DI + middleware
- Tenant resolution
- Permission caching (singleton)
- Authorization requirement + handler
- Dynamic policy usage

--------------------------
Link a user to a tenant.
link a user to a tenant-system
link a user to a tenant (system-app) 

- first user registers and logs in  (Master , AspNet User)
- select a tenant and send a request to join, request is created in the tenant database table AspNetUserRequests
- in tenant database AspNetUserRequest (AspNetUserId, Date requested, Status) status: waiting, accepted, rejected
 tenant if accept: 
 operation in tenant, in tenant database: create AuthUser, Close request, assign Roles
 operation in tenant, in master database: add claim AspnetUserClaim with the tenantId
 
 sql script:
when adding a new tenant
- add user: Bfs Admin Randa, Suzette to the following tables:
- athUser using the same aspnetId as in master
- athRoleUser the using athUser.Id link to the RoleId = 1
- dont add the role bfsadmin to the athRole